## 구조

main.js 와 app.js 의 역할을 좀 알 것 같다.
main.js 는 Root 객체를 생성하여 render entry point 를 제공하는 역할,
app.js 는 페이지 조립의 역할을 하는 것 같다.

## 모듈화

button.js 를 공통 컴포넌트로 분리하니까 매우 편해졌다.
다른 것들도 재사용 가능한 지 고민하게 된다.

## 일관성

일관성 있는 코드를 작성하려 노력하다보니 코드가 읽기 쉬워지고 다음 코드를 예측하기도 쉬워지는 것 같다.
convention 만 잘 짜도 반은 가는 것 같다.