## 배열 복사와 내부 객체 복사 

```
const a = original
// 배열 공유, 내부 객체 공유

const b = [...original]
// 배열 분리, 내부 객체 공유

const c = original.map(item => ({ ...item }))
// 배열 분리, 최상위 내부 객체도 분리

const d = structuredClone(original)
// 지원되는 모든 중첩 데이터까지 깊게 분리
```
