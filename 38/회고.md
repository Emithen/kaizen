## never

[] 를 발견하면 대입이나 사용 등을 확인하고, 없으면 never 로 추론한다.
never 는 어느 값도 허용하지 않는 공집합이다.

## 함수 시그니처

함수 시그니처를 복잡하게 선언하고 단조롭게 대입하는 것은 문제가 되지 않는다.
선언한 것보다 많은 것을 포함하여 대입하게 될 경우 문제가 발생한다.

## type 정의

type 이 정의되지 않은 요소를 호출할 경우 호출부에서의 type 에러가 발견되지 못한다.
any 에 지워지기 때문.
