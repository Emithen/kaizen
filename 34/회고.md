## await

await 은 Promise 가 아닌 대상을 Promise 로 감싼다.
await 은 rejected Promise 를 만나면 그 자리에서 throw 한다.


## async

하는 일: await 허가, 반환값을 Promise 로 포장
반환값이 이미 Promise 객체일 경우 재포장 없이 단순 채택한다.


## fetch

반환형: Promise<Response>


## Response

json()
